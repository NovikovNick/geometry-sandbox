# Template for using Raylib, using CMake's FetchContent.

- ``build_and_deploy_on_localhost.bat`` will complile with emscripten and deploy on localhost:8000
- ``build_and_run_on_windows.bat`` complile with default and execute